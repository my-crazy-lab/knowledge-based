#!/usr/bin/env node

/**
 * Tiny Package Manager (TPM)
 * A simple demonstration of how package managers work
 */

const PackageManager = require('./src/PackageManager');

module.exports = PackageManager;
